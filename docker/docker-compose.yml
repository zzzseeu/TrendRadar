services:
  trendradar:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        HTTP_PROXY: ${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
        HTTPS_PROXY: ${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
        http_proxy: ${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
        https_proxy: ${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
    container_name: trendradar
    restart: unless-stopped

    ports:
      - "127.0.0.1:${WEBSERVER_PORT:-8080}:${WEBSERVER_PORT:-8080}"

    volumes:
      - ../config:/app/config:ro
      - ../output:/app/output

    extra_hosts:
      - "host.docker.internal:host-gateway"

    environment:
      - TZ=Asia/Shanghai
      # 宿主机代理（容器内不能使用 127.0.0.1 访问宿主机）
      - HTTP_PROXY=${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
      - HTTPS_PROXY=${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
      - http_proxy=${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
      - https_proxy=${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
      # 新闻抓取先直连；仅失败时显式使用此代理回退
      - NEWS_PROXY_URL=${DOCKER_PROXY_URL:-http://host.docker.internal:7892}
      # AI 内网网关直连，新闻抓取等其他外网请求继续使用上述代理
      - NO_PROXY=${DOCKER_NO_PROXY:-apigw.hnaicc.cn,qyapi.weixin.qq.com}
      - no_proxy=${DOCKER_NO_PROXY:-apigw.hnaicc.cn,qyapi.weixin.qq.com}
      # Web 服务器
      - WEBSERVER_PORT=${WEBSERVER_PORT:-8080}
      # 通知渠道
      - FEISHU_WEBHOOK_URL=${FEISHU_WEBHOOK_URL:-}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID:-}
      - DINGTALK_WEBHOOK_URL=${DINGTALK_WEBHOOK_URL:-}
      - WEWORK_WEBHOOK_URL=${WEWORK_WEBHOOK_URL:-}
      - WEWORK_MSG_TYPE=${WEWORK_MSG_TYPE:-}
      # 邮件配置
      - EMAIL_FROM=${EMAIL_FROM:-}
      - EMAIL_PASSWORD=${EMAIL_PASSWORD:-}
      - EMAIL_TO=${EMAIL_TO:-}
      - EMAIL_SMTP_SERVER=${EMAIL_SMTP_SERVER:-}
      - EMAIL_SMTP_PORT=${EMAIL_SMTP_PORT:-}
      # ntfy配置
      - NTFY_SERVER_URL=${NTFY_SERVER_URL:-https://ntfy.sh}
      - NTFY_TOPIC=${NTFY_TOPIC:-}
      - NTFY_TOKEN=${NTFY_TOKEN:-}
      # Bark配置
      - BARK_URL=${BARK_URL:-}
      # Slack配置
      - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL:-}
      # 通用Webhook配置
      - GENERIC_WEBHOOK_URL=${GENERIC_WEBHOOK_URL:-}
      - GENERIC_WEBHOOK_TEMPLATE=${GENERIC_WEBHOOK_TEMPLATE:-}
      # AI 配置（ai_analysis 和 ai_translation 共享模型配置）
      - AI_ANALYSIS_ENABLED=${AI_ANALYSIS_ENABLED:-}
      - AI_API_KEY=${AI_API_KEY:-}
      - AI_MODEL=${AI_MODEL:-}
      - AI_API_BASE=${AI_API_BASE:-}
      # Elsevier 全文 API 凭据（仅服务端使用）
      - ELSEVIER_API_KEY=${ELSEVIER_API_KEY:-}
      - ELSEVIER_INST_TOKEN=${ELSEVIER_INST_TOKEN:-}
      # 远程存储配置（S3 兼容协议）
      - S3_ENDPOINT_URL=${S3_ENDPOINT_URL:-}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME:-}
      - S3_ACCESS_KEY_ID=${S3_ACCESS_KEY_ID:-}
      - S3_SECRET_ACCESS_KEY=${S3_SECRET_ACCESS_KEY:-}
      - S3_REGION=${S3_REGION:-}
      # 运行模式
      - CRON_SCHEDULES=${CRON_SCHEDULES:-}
      - CRON_SCHEDULE=${CRON_SCHEDULE:-}
      - RUN_MODE=${RUN_MODE:-cron}
      - IMMEDIATE_RUN=${IMMEDIATE_RUN:-true}

  trendradar-mcp:
    image: wantcat/trendradar-mcp:latest
    container_name: trendradar-mcp
    restart: unless-stopped

    ports:
      - "${MCP_HOST:-127.0.0.1}:${MCP_PORT:-3333}:3333"

    volumes:
      - ../config:/app/config:ro
      - ../output:/app/output

    environment:
      - TZ=Asia/Shanghai
      - MCP_PORT=${MCP_PORT:-3333}
